{
  "name": "tests-jarvis-views",
  "version": "1.0.0",
  "description": "tests the front end UI for jarvis assistant",
  "main": "test.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "codeZero",
  "license": "ISC",
  "dependencies": {
    "mocha": "^6.0.2",
    "selenium-webdriver": "^4.0.0-alpha.1"
  }
}
