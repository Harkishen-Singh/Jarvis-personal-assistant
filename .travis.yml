os:
  - linux

sudo: required

services:
  - xvfb

language:
  - go

go:
  - 1.11.x

addons:
  chrome: stable
  apt:
    packages:
      - chromium-chromedriver

cache:
  - tests/node_modules/

before-install:
  - . $HOME/.nvm/nvm.sh
  - nvm install stable
  - nvm use stable
  - npm install -g http-server mocha eslint yarn
  - cd service/ && go get -u -v -d && cd ..
  - cd tests/ && npm install && npm install chromedriver geckodriver && cd ..


script:
  - make build
  - ./bin/service &
  - export ENV=test
  - eslint view/web/app-jarvis.js
  - echo "checking js format ..."
  - yarn ci-lint
  - cd view/web/ && npm start && cd ../..
  - go test service/controllers/controllers_test.go
  - cd tests/
  - npm install
  - xvfb-run -s "-screen 0 1920x1080x16" -a mocha test.js